# Placeholder photos

The 16 photos in `win/` and `loss/` are temporary placeholders from a single match
(BCC v Hatherleigh, 25 May 2024), shared via Google Drive. The actual result of
that match is unknown to us, so they are **not** sorted by real outcome — they're
split by visual energy instead: punchier action shots (full swings, running, ball
mid-flight) went to `win/`, calmer shots (players chatting, spectators, defensive
blocks) went to `loss/`.

Replace these with a properly curated set of real win/loss photos before going live.
