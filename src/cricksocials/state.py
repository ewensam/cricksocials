"""Processed-match state tracker (JSON file).

Phase 9 will implement the full state management logic.
"""

from __future__ import annotations

# TODO (Phase 9): Implement StateStore class:
#   - load(path) -> StateStore
#   - is_processed(match_id) -> bool
#   - mark_processed(match_id, metadata) -> None
#   - save() -> None  (atomic write via temp file + rename)
